[agent]
  interval = "10s"
  flush_interval = "5s"
  omit_hostname = true
  skip_processors_after_aggregators = false

# ================= Saída: InfluxDB 2.x =================
[[outputs.influxdb_v2]]
  urls = ["http://influxdb:8086"]
  token = "${INFLUX_TOKEN}"
  organization = "${INFLUX_ORG}"
  bucket = "${INFLUX_BUCKET}"

# ================= Entrada: MQTT (Mosquitto) =================
[[inputs.mqtt_consumer]]
  servers = ["tcp://mosquitto:1883"]
  topics = ["${MQTT_TOPIC}"]
  client_id = "telegraf-cultivia"
  qos = 0
  topic_tag = "topic"

  # Measurement no InfluxDB
  name_override = "estufa"

  # Payload publicado pelo Pico (a cada 2s no modo joystick):
  # {"modo": "joystick", "temperatura_simulada": float, "umidade_simulada": float,
  #  "botao_a": 0|1, "botao_c": 0|1, "joystick_sw": 0|1}
  # Ao entrar no modo microfone, o Pico publica só {"modo": "microfone"} e pausa
  # os envios — por isso todos os campos são opcionais.
  data_format = "json_v2"
  [[inputs.mqtt_consumer.json_v2]]
    [[inputs.mqtt_consumer.json_v2.field]]
      path = "modo"
      type = "string"
      optional = true
    [[inputs.mqtt_consumer.json_v2.field]]
      path = "temperatura_simulada"
      type = "float"
      optional = true
    [[inputs.mqtt_consumer.json_v2.field]]
      path = "umidade_simulada"
      type = "float"
      optional = true
    [[inputs.mqtt_consumer.json_v2.field]]
      path = "botao_a"
      type = "int"
      optional = true
    [[inputs.mqtt_consumer.json_v2.field]]
      path = "botao_c"
      type = "int"
      optional = true
    [[inputs.mqtt_consumer.json_v2.field]]
      path = "joystick_sw"
      type = "int"
      optional = true
